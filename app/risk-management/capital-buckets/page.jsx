import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("capital-buckets");

export default function Page() {
  return <ChapterView slug="capital-buckets" />;
}
