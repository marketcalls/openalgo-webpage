import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("investor-mistakes");

export default function Page() {
  return <ChapterView slug="investor-mistakes" />;
}
