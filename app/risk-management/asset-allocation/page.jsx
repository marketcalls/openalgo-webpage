import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("asset-allocation");

export default function Page() {
  return <ChapterView slug="asset-allocation" />;
}
