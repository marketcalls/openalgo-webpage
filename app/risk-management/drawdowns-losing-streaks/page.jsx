import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("drawdowns-losing-streaks");

export default function Page() {
  return <ChapterView slug="drawdowns-losing-streaks" />;
}
