import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("risk-capacity-vs-tolerance");

export default function Page() {
  return <ChapterView slug="risk-capacity-vs-tolerance" />;
}
