import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("option-seller-risk");

export default function Page() {
  return <ChapterView slug="option-seller-risk" />;
}
