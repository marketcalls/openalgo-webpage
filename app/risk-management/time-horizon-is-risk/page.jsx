import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("time-horizon-is-risk");

export default function Page() {
  return <ChapterView slug="time-horizon-is-risk" />;
}
