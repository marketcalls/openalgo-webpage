import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("risk-reward-win-rate");

export default function Page() {
  return <ChapterView slug="risk-reward-win-rate" />;
}
