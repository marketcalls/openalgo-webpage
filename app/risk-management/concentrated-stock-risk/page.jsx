import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("concentrated-stock-risk");

export default function Page() {
  return <ChapterView slug="concentrated-stock-risk" />;
}
