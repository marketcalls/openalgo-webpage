import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("portfolio-heat");

export default function Page() {
  return <ChapterView slug="portfolio-heat" />;
}
