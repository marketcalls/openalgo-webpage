import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("the-risk-pyramid");

export default function Page() {
  return <ChapterView slug="the-risk-pyramid" />;
}
