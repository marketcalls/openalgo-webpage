import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("expiry-day-risk");

export default function Page() {
  return <ChapterView slug="expiry-day-risk" />;
}
