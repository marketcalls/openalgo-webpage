import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("what-risk-really-means");

export default function Page() {
  return <ChapterView slug="what-risk-really-means" />;
}
