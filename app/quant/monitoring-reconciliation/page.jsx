import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("monitoring-reconciliation");

export default function Page() {
  return <ChapterView slug="monitoring-reconciliation" />;
}
