import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("stationarity-random-walk");

export default function Page() {
  return <ChapterView slug="stationarity-random-walk" />;
}
