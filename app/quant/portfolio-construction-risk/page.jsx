import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("portfolio-construction-risk");

export default function Page() {
  return <ChapterView slug="portfolio-construction-risk" />;
}
