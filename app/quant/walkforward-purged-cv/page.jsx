import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("walkforward-purged-cv");

export default function Page() {
  return <ChapterView slug="walkforward-purged-cv" />;
}
