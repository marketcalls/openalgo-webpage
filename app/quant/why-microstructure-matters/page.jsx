import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("why-microstructure-matters");

export default function Page() {
  return <ChapterView slug="why-microstructure-matters" />;
}
