import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("futures-and-carry");

export default function Page() {
  return <ChapterView slug="futures-and-carry" />;
}
