import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("bulk-block-deals");

export default function Page() {
  return <ChapterView slug="bulk-block-deals" />;
}
