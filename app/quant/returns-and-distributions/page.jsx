import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("returns-and-distributions");

export default function Page() {
  return <ChapterView slug="returns-and-distributions" />;
}
