import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("realized-volatility");

export default function Page() {
  return <ChapterView slug="realized-volatility" />;
}
