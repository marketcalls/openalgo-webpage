import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("mean-reversion-cointegration");

export default function Page() {
  return <ChapterView slug="mean-reversion-cointegration" />;
}
