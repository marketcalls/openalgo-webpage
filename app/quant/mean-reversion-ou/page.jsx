import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("mean-reversion-ou");

export default function Page() {
  return <ChapterView slug="mean-reversion-ou" />;
}
