import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("inference-and-overfitting");

export default function Page() {
  return <ChapterView slug="inference-and-overfitting" />;
}
