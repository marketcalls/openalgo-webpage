import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("clearing-and-settlement");

export default function Page() {
  return <ChapterView slug="clearing-and-settlement" />;
}
