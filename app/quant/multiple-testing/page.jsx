import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("multiple-testing");

export default function Page() {
  return <ChapterView slug="multiple-testing" />;
}
