import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("probability-and-monte-carlo");

export default function Page() {
  return <ChapterView slug="probability-and-monte-carlo" />;
}
