import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("momentum-and-value");

export default function Page() {
  return <ChapterView slug="momentum-and-value" />;
}
