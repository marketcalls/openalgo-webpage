import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("orderbook-reconstruction");

export default function Page() {
  return <ChapterView slug="orderbook-reconstruction" />;
}
