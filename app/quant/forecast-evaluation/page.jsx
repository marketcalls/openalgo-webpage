import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("forecast-evaluation");

export default function Page() {
  return <ChapterView slug="forecast-evaluation" />;
}
