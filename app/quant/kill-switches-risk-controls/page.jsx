import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("kill-switches-risk-controls");

export default function Page() {
  return <ChapterView slug="kill-switches-risk-controls" />;
}
