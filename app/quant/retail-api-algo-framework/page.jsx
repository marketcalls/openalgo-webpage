import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("retail-api-algo-framework");

export default function Page() {
  return <ChapterView slug="retail-api-algo-framework" />;
}
