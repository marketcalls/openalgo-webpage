import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("distributions-fat-tails");

export default function Page() {
  return <ChapterView slug="distributions-fat-tails" />;
}
