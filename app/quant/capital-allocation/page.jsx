import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("capital-allocation");

export default function Page() {
  return <ChapterView slug="capital-allocation" />;
}
