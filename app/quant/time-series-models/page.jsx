import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("time-series-models");

export default function Page() {
  return <ChapterView slug="time-series-models" />;
}
