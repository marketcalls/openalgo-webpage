import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("price-time-priority");

export default function Page() {
  return <ChapterView slug="price-time-priority" />;
}
