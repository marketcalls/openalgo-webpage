import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("regimes-and-breaks");

export default function Page() {
  return <ChapterView slug="regimes-and-breaks" />;
}
