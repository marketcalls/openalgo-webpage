import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("flow-based-strategies");

export default function Page() {
  return <ChapterView slug="flow-based-strategies" />;
}
