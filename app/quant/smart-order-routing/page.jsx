import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("smart-order-routing");

export default function Page() {
  return <ChapterView slug="smart-order-routing" />;
}
