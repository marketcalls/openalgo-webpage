import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("continuation-patterns");

export default function Page() {
  return <ChapterView slug="continuation-patterns" />;
}
