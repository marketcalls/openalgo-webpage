import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("reversal-patterns");

export default function Page() {
  return <ChapterView slug="reversal-patterns" />;
}
