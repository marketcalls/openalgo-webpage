import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("multi-candle-patterns");

export default function Page() {
  return <ChapterView slug="multi-candle-patterns" />;
}
