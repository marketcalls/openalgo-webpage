import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("moving-averages");

export default function Page() {
  return <ChapterView slug="moving-averages" />;
}
