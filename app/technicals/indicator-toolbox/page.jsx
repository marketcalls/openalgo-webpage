import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("indicator-toolbox");

export default function Page() {
  return <ChapterView slug="indicator-toolbox" />;
}
