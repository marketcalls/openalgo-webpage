import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("timeframes-and-scales");

export default function Page() {
  return <ChapterView slug="timeframes-and-scales" />;
}
