import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("risk-management");

export default function Page() {
  return <ChapterView slug="risk-management" />;
}
