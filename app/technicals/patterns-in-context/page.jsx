import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("patterns-in-context");

export default function Page() {
  return <ChapterView slug="patterns-in-context" />;
}
