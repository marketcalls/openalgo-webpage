import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("support-and-resistance");

export default function Page() {
  return <ChapterView slug="support-and-resistance" />;
}
