import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("single-candle-patterns");

export default function Page() {
  return <ChapterView slug="single-candle-patterns" />;
}
