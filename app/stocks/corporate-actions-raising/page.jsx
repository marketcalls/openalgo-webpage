import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("corporate-actions-raising");

export default function Page() {
  return <ChapterView slug="corporate-actions-raising" />;
}
