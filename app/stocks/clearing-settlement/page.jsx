import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("clearing-settlement");

export default function Page() {
  return <ChapterView slug="clearing-settlement" />;
}
