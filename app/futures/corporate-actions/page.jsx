import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("corporate-actions");

export default function Page() {
  return <ChapterView slug="corporate-actions" />;
}
