import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("basis-and-carry");

export default function Page() {
  return <ChapterView slug="basis-and-carry" />;
}
