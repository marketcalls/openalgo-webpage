import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("why-futures-exist");

export default function Page() {
  return <ChapterView slug="why-futures-exist" />;
}
