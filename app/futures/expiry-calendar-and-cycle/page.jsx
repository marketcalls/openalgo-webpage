import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("expiry-calendar-and-cycle");

export default function Page() {
  return <ChapterView slug="expiry-calendar-and-cycle" />;
}
