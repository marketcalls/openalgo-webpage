import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("open-interest-in-futures");

export default function Page() {
  return <ChapterView slug="open-interest-in-futures" />;
}
