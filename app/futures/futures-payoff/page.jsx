import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("futures-payoff");

export default function Page() {
  return <ChapterView slug="futures-payoff" />;
}
