import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("settlement-stock-vs-index");

export default function Page() {
  return <ChapterView slug="settlement-stock-vs-index" />;
}
