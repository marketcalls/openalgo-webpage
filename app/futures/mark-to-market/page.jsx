import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("mark-to-market");

export default function Page() {
  return <ChapterView slug="mark-to-market" />;
}
