import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("gap-and-event-risk");

export default function Page() {
  return <ChapterView slug="gap-and-event-risk" />;
}
