import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("contract-specs");

export default function Page() {
  return <ChapterView slug="contract-specs" />;
}
