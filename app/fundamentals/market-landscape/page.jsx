import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("market-landscape");

export default function Page() {
  return <ChapterView slug="market-landscape" />;
}
