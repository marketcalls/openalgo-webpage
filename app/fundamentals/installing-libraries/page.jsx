import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("installing-libraries");

export default function Page() {
  return <ChapterView slug="installing-libraries" />;
}
