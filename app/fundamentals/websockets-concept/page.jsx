import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("websockets-concept");

export default function Page() {
  return <ChapterView slug="websockets-concept" />;
}
