import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("comprehensions");

export default function Page() {
  return <ChapterView slug="comprehensions" />;
}
