import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("dates-in-pandas");

export default function Page() {
  return <ChapterView slug="dates-in-pandas" />;
}
