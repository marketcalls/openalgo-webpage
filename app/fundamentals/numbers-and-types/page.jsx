import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("numbers-and-types");

export default function Page() {
  return <ChapterView slug="numbers-and-types" />;
}
