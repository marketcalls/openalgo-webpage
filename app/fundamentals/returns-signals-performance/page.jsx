import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("returns-signals-performance");

export default function Page() {
  return <ChapterView slug="returns-signals-performance" />;
}
