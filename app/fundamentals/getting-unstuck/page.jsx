import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("getting-unstuck");

export default function Page() {
  return <ChapterView slug="getting-unstuck" />;
}
