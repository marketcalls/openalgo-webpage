import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("symbols-exchanges");

export default function Page() {
  return <ChapterView slug="symbols-exchanges" />;
}
