import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("intraday-strategies");

export default function Page() {
  return <ChapterView slug="intraday-strategies" />;
}
