import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("risk-position-sizing");

export default function Page() {
  return <ChapterView slug="risk-position-sizing" />;
}
