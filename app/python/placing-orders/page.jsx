import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("placing-orders");

export default function Page() {
  return <ChapterView slug="placing-orders" />;
}
