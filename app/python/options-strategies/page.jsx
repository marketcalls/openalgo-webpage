import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("options-strategies");

export default function Page() {
  return <ChapterView slug="options-strategies" />;
}
