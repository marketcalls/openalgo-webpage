import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("performance-metrics");

export default function Page() {
  return <ChapterView slug="performance-metrics" />;
}
