import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("historical-data");

export default function Page() {
  return <ChapterView slug="historical-data" />;
}
