import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("seasonal-strategies");

export default function Page() {
  return <ChapterView slug="seasonal-strategies" />;
}
