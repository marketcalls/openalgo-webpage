import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("volume-indicators");

export default function Page() {
  return <ChapterView slug="volume-indicators" />;
}
