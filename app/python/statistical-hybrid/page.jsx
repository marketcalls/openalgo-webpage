import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("statistical-hybrid");

export default function Page() {
  return <ChapterView slug="statistical-hybrid" />;
}
