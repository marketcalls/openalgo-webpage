import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("when-not-to-hedge");

export default function Page() {
  return <ChapterView slug="when-not-to-hedge" />;
}
