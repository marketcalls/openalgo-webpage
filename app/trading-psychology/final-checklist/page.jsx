import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("final-checklist");

export default function Page() {
  return <ChapterView slug="final-checklist" />;
}
