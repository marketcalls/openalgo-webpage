import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("hedge-ratio-portfolio");

export default function Page() {
  return <ChapterView slug="hedge-ratio-portfolio" />;
}
