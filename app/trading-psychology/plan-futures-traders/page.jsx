import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("plan-futures-traders");

export default function Page() {
  return <ChapterView slug="plan-futures-traders" />;
}
