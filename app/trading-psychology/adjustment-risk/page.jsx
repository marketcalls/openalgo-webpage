import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("adjustment-risk");

export default function Page() {
  return <ChapterView slug="adjustment-risk" />;
}
