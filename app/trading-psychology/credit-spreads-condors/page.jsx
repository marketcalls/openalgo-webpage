import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("credit-spreads-condors");

export default function Page() {
  return <ChapterView slug="credit-spreads-condors" />;
}
