import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("the-strategy-builder");

export default function Page() {
  return <ChapterView slug="the-strategy-builder" />;
}
