import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("reading-payoffs");

export default function Page() {
  return <ChapterView slug="reading-payoffs" />;
}
