import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("options-adjustments");

export default function Page() {
  return <ChapterView slug="options-adjustments" />;
}
