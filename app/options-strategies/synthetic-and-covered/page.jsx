import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("synthetic-and-covered");

export default function Page() {
  return <ChapterView slug="synthetic-and-covered" />;
}
