import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("backtest-report");

export default function Page() {
  return <ChapterView slug="backtest-report" />;
}
