import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("referencing-bars");

export default function Page() {
  return <ChapterView slug="referencing-bars" />;
}
