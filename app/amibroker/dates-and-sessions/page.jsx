import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("dates-and-sessions");

export default function Page() {
  return <ChapterView slug="dates-and-sessions" />;
}
