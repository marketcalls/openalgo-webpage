import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("arrays-and-loops");

export default function Page() {
  return <ChapterView slug="arrays-and-loops" />;
}
