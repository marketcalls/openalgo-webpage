import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("built-in-functions");

export default function Page() {
  return <ChapterView slug="built-in-functions" />;
}
