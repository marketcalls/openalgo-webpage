import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("exploration-basics");

export default function Page() {
  return <ChapterView slug="exploration-basics" />;
}
