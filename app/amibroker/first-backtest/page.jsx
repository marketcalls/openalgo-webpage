import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("first-backtest");

export default function Page() {
  return <ChapterView slug="first-backtest" />;
}
