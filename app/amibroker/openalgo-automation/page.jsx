import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("openalgo-automation");

export default function Page() {
  return <ChapterView slug="openalgo-automation" />;
}
