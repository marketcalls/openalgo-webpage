import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("custom-functions");

export default function Page() {
  return <ChapterView slug="custom-functions" />;
}
