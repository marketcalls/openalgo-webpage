import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("position-sizing");

export default function Page() {
  return <ChapterView slug="position-sizing" />;
}
