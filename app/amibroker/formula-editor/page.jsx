import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("formula-editor");

export default function Page() {
  return <ChapterView slug="formula-editor" />;
}
