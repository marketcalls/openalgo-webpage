import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("panes-and-foreign");

export default function Page() {
  return <ChapterView slug="panes-and-foreign" />;
}
