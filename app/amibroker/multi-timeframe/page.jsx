import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("multi-timeframe");

export default function Page() {
  return <ChapterView slug="multi-timeframe" />;
}
