import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("stops-and-targets");

export default function Page() {
  return <ChapterView slug="stops-and-targets" />;
}
