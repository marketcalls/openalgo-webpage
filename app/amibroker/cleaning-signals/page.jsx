import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("cleaning-signals");

export default function Page() {
  return <ChapterView slug="cleaning-signals" />;
}
