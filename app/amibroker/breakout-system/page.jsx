import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("breakout-system");

export default function Page() {
  return <ChapterView slug="breakout-system" />;
}
