import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("rsi-pullback-system");

export default function Page() {
  return <ChapterView slug="rsi-pullback-system" />;
}
