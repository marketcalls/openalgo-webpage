import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("stationarity-and-random-walks");

export default function Page() {
  return <ChapterView slug="stationarity-and-random-walks" />;
}
