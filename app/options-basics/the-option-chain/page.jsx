import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("the-option-chain");

export default function Page() {
  return <ChapterView slug="the-option-chain" />;
}
