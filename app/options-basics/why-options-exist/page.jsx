import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("why-options-exist");

export default function Page() {
  return <ChapterView slug="why-options-exist" />;
}
