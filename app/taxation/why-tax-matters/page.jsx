import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("why-tax-matters");

export default function Page() {
  return <ChapterView slug="why-tax-matters" />;
}
