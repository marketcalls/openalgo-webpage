import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("not-reporting-fno");

export default function Page() {
  return <ChapterView slug="not-reporting-fno" />;
}
