import ChapterView, { chapterMeta } from "../_ChapterView";

export const metadata = chapterMeta("advance-tax-and-deadlines");

export default function Page() {
  return <ChapterView slug="advance-tax-and-deadlines" />;
}
