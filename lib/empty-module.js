// Intentionally empty. Used by next.config.mjs to replace Next.js's
// polyfill-module (Array.prototype.at/flat, Object.fromEntries/hasOwn), which our
// browserslist targets all support natively, so shipping the polyfills would be
// serving legacy JavaScript to modern browsers.
export {}
